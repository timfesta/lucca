	<div class="row">
	<div class="col-md-6 col-md-offset-3">
		<%= form_for @user do |f| %>
		<%= f.text_field :name, placeholder: "Name" %>
		<%= f.email_field :email, placeholder: "Emial" %>
		<%= f.submit "Sign Up" %>
		<% end %>
	</div>
</div>

